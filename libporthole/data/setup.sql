CREATE TABLE IF NOT EXISTS ph_ecache (
	id INTEGER PRIMARY KEY,
	cat TEXT,
	pkg TEXT,
	ver TEXT,
	repo TEXT,
	
	-- ebuild cache data
	EAPI INTEGER,
	DESCRIPTION TEXT,
	BDEPEND TEXT,
	HOMEPAGE TEXT,
	IDEPEND TEXT,
	INHERIT TEXT,
	IUSE TEXT,
	KEYWORDS TEXT,
	LICENSE TEXT,
	RDEPEND TEXT,
	REQUIRED_USE TEXT,
	RESTRICT TEXT,
	SLOT TEXT,
	SRC_URI TEXT
);

CREATE INDEX IF NOT EXISTS ph_ecache_lookup_idx
ON ph_ecache (cat, pkg);
